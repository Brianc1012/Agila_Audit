# ────────────────────────────────
# 🔧 Server Configuration
# ────────────────────────────────
PORT=4003
HOST=localhost
BASE_URL=http://localhost:4003
# Note: Express now serves both API and Next.js UI on same port
# No separate BACKEND_URL needed for integrated mode

# ────────────────────────────────
# 🗄️ Database Connection
# ────────────────────────────────
AUDIT_LOGS_DATABASE_URL="postgresql://postgres:nerieannganda247@localhost:5432/audit_db2?schema=public"
SHADOW_DATABASE_URL="postgresql://postgres:nerieannganda247@localhost:5432/audit_shadow_db1?schema=public"

# ────────────────────────────────
# 🔐 Security and Auth Config
# ────────────────────────────────
JWT_SECRET=

# Default raw keys for departments (used in seeder)
FINANCE_API_KEY=FINANCE_DEFAULT_KEY
HR_API_KEY=HR_DEFAULT_KEY
INVENTORY_API_KEY=INVENTORY_DEFAULT_KEY
OPERATIONS_API_KEY=OPERATIONS_DEFAULT_KEY

# For clarity
SERVICE_NAME=audit-logs-microservice
PRISMA_LOG_LEVEL=info


# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="postgresql://johndoe:randompassword@localhost:5432/mydb?schema=public"